[DEFAULT]
test1 = param1
test2 = param2

[DB]
# database credentials (user should have access to create and delete database)
host = localhost
database = testtclopslib
username = root
password = root
port = 3306

[log]
# test log file
file = /tmp/test-tcl-shared-libs.log

[ssh]
# ssh host which permit cannection from current user using ssh key without password
# current user should have .bachrc file in the home directory
host = dev.ops-dev2

[ssh-password]
# ssh host hich permit connection from specified user and ask password
# specified user should have .bashrc file in the home directory
host = localhost
username = mtiurin
port = 22
